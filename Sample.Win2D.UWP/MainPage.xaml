<Page
    x:Class="Sample.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:TBFX="using:TextBlockFX.Win2D.UWP"
    xmlns:controls="using:Sample.Win2D.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:effects="using:TextBlockFX.Win2D.UWP.Effects"
    xmlns:local="using:Sample.UWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
    mc:Ignorable="d">

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="5">
            <Button
                MinWidth="250"
                MinHeight="80"
                Background="{ThemeResource InkToolbarAccentColorThemeBrush}"
                Command="{x:Bind GotoAnimateCommand}"
                Content="Animated Text Effects" />
            <Button
                MinWidth="250"
                MinHeight="80"
                Background="DarkCyan"
                Command="{x:Bind GotoTextEffectsCommand}"
                Content="Text Effects" />
        </StackPanel>
    </Grid>
</Page>
